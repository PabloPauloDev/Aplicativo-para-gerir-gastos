import {COLORS, FONT_SIZE} from "../../constants/themes.js"

export const styles = {
    despesa: {
        width: "100%",
        backgroundColor: COLORS.gray,
        padding: 12,
        borderRadius: 10,
        marginTop: 10,
        flexDirection: "row"
    }
}
